# Pin version đã test (Python 3.12). Họ LangChain đổi API liên tục nên pin chặt.
# Cài: uv pip install -r requirements.txt   (hoặc pip install -r requirements.txt)

# Agent + LLM
langchain-core
langchain-openai
langchain-text-splitters
langgraph
langgraph-prebuilt
openai

# Vector store + embedding
sqlite-vec
sentence-transformers
numpy
# Document parse
pypdf

# API
fastapi
uvicorn[standard]
python-multipart
pydantic

# Dev
pytest
